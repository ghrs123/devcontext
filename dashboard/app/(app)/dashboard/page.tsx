export default function DashboardPage() {
  return (
    <main className="mx-auto max-w-6xl px-4 py-10">
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-muted-foreground">Authenticated area is ready. Prompt 7.2 will add the full app shell and analytics overview.</p>
    </main>
  );
}
