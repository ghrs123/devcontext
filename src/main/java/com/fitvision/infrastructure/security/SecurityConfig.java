package com.fitvision.infrastructure.security;

// Spring Security configuration.
// Phase 1 will wire API key filter (widget) and JWT filter (dashboard).
// Widget endpoints are public; dashboard endpoints require JWT auth.
